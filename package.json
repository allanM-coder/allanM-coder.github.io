{
  "name": "allan-coaching-cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test worker/index.test.mjs",
    "check": "wrangler deploy --dry-run --outdir /tmp/allan-coaching-worker-check"
  },
  "devDependencies": {
    "wrangler": "4.120.0"
  }
}
